<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8" />
<title>Assistant IA Sécurité Privée</title>
<style>
<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8" />
<title>Assistant IA Sécurité Privée</title>
<style>
  body { font-family: Arial, sans-serif; max-width: 600px; margin: 2em auto; }
  #chat { border: 1px solid #ccc; padding: 1em; height: 300px; overflow-y: auto; background: #f9f9f9; }
  #input { width: 100%; padding: 0.5em; margin-top: 1em; box-sizing: border-box; }
  .user { color: blue; margin-bottom: 0.5em; }
  .bot { color: green; margin-bottom: 1em; white-space: pre-wrap; }
</style>
</head>
<body>

<h2>Assistant IA Sécurité Privée</h2>
<div id="chat"></div>
<input id="input" placeholder="Pose ta question..." autofocus autocomplete="off" />

<script>
const chat = document.getElementById('chat');
const input = document.getElementById('input');

function addMessage(text, sender) {
  const div = document.createElement('div');
  div.className = sender;
  div.textContent = text;
  chat.appendChild(div);
  chat.scrollTop = chat.scrollHeight;
}

input.addEventListener('keydown', async (e) => {
  if (e.key === 'Enter' && input.value.trim()) {
    const question = input.value.trim();
    addMessage('Tu : ' + question, 'user');
    input.value = '';
    addMessage('...', 'bot');

    try {
      const res = await fetch('https://premarnaud.app.n8n.cloud/webhook-test/chatbot-securite', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({ question })
      });
      const data = await res.json();
      chat.lastChild.textContent = 'Bot : ' + (data.answer || 'Pas de réponse.');
    } catch (err) {
      chat.lastChild.textContent = 'Bot : Erreur serveur.';
    }
  }
});
</script>

</body>
</html>
